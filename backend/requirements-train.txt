# ── Full ML pipeline (local only — NOT needed for deployment) ─────────────────
# Install this to retrain models / regenerate data/processed/*.parquet:
#   pip install -r requirements-train.txt
-r requirements.txt
xgboost>=2.0
scikit-learn>=1.3
prophet>=1.1
tensorflow-cpu>=2.15
shap>=0.44
scipy>=1.11
