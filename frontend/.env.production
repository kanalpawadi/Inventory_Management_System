# Production environment — used by `vite build`
# Fallback API URL for `vite build`. On Render the VITE_API_URL env var set by
# render.yaml takes priority over this file (real env vars beat .env files).
VITE_API_URL=https://demand-iq-api.onrender.com
